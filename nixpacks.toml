[phases.setup]
commands = ["npm ci"]

[phases.build]
commands = ["npm run build"]

[phases.start]
commands = ["npx prisma migrate deploy && echo '✅ Migrations completed'", "echo '🚀 Starting seeding...' && npm run prisma:seed && echo '✅ Seeding completed' || echo '❌ Seeding failed'", "echo '🚀 Starting server...' && node dist/server.js"]
